server:
  port: 80
spring:
  datasource:
    url: jdbc:mysql://106.53.153.92:3306/db_mblog?charset=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root2021
#  data:
#    elasticsearch:
#         cluster-nodes: localhost:9300
#         cluster-name: es-wyf



mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 开启sql打印
  global-config:
    db-config:
      logic-delete-field: deleted # 逻辑删除字段名
      logic-delete-value: 1  # 逻辑删除值
      logic-not-delete-value: 0 # 未删除值
logging:
  level:
    root: info
    com.ithzk.es: debug



